# <%= resource_name %>

[Back to resource list](../README.md#resources)
<% unless nil_or_empty_any?(uses) %>

## Uses

<% uses.each do |u| %>
- [<%= u %>](<%= u %>.md)
<% end %>
<% end %>

## Actions

<% if nil_or_empty_any?(actions) %>
- None
<% else %>
<% actions.each do |a| %>
- `:<%= a %>`
<% end %>
<% end %>

## Properties

| Name                   | Name? | Type                   | Default                          | Description                                      | Allowed Values       |
| ---------------------- | ----- | ---------------------- | -------------------------------- | -------------------------------------------------| -------------------- |
<% unless nil_or_empty_any?(properties) %>
<% properties.each do |p| %>
|<%= "`#{p[:name]}`".ljust(24) %>|<% if p[:name_property] %><%= "\u2713".encode('utf-8').center(7) %><% end %>|<%= p.fetch(:type, []).join(', ').ljust(24) %>|<%= ''.ljust(34) %>|<%= p.fetch(:description, '').ljust(50) %>|<%= "#{p.fetch(:equal_to, []).join(', ')}".ljust(22) %>|
<% end %>
<% end %>
<% unless nil_or_empty_any?(libraries) %>

## Libraries

<% libraries.each do |l| %>
- `<%= l %>`
<% end %>
<% end %>
